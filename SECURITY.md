# Security

如果你发现了安全问题，请不要直接公开提交利用细节。

建议最小化提供：

- 影响范围
- 复现步骤
- 是否需要真实写权限
- 是否涉及本机命令执行、文件越权、会话泄露或路径泄露

本项目默认安全边界：

- 服务仅绑定 `127.0.0.1`
- POST 接口可通过 `TASK_DASHBOARD_TOKEN` 保护
- 文件 reveal 仅允许工作区内路径
- 默认执行模式为 `sandboxed`
