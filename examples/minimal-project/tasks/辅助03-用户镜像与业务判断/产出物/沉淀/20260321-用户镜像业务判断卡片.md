# 用户镜像业务判断卡片

## 这个示例项目在帮谁

- 帮第一次接触 Qoreon 的用户
- 帮用户自己的 AI 先学会最小协作
- 帮用户用公开安全方式复制一个新项目

## 为什么保留用户镜像通道

- 让 AI 不只会执行，还能先判断“这样做对用户有没有意义”
- 在示例项目里提醒主线和支撑的区别
- 防止一开始就把所有精力放到 Git 或结构细节上

## 默认建议

- 首轮保留用户镜像通道
- 但它只做判断支撑，不替总控直接派发
