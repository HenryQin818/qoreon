# 总控开箱路径收口基线

## 当前结论

- 公开示例项目默认采用 `2 + 4 + X`
- 用户电脑上的 AI 应先跑 bootstrap，再进入总控任务
- 第一轮不要全通道并发，先由总控判断主线

## 总控默认顺序

1. 读取 `docs/public/ai-bootstrap.md`
2. 读取 `examples/minimal-project/README.md`
3. 读取架构与用户镜像沉淀
4. 决定首批执行通道
5. 汇总测试结论后再决定是否放行
