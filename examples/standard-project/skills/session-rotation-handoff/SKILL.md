# session-rotation-handoff

## 用途

用于会话过长、上下文腐烂或旧会话不再可靠时的接力切换。

## 适用场景

- 某个 Agent 长期运行后质量下降
- 当前会话无法继续压缩
- 需要新会话接替旧会话

## 步骤

1. 先确认是否真的需要轮换
2. 给出旧会话的职责摘要
3. 创建新会话并完成最小培训
4. 更新默认入口和通讯录
5. 通知原协作方切换到新会话

## 输出

- 当前结论
- 是否通过或放行
- 唯一阻塞
- 关键路径或 run_id
- 下一步动作

## 边界

- 不直接丢弃旧会话的职责信息
- 不在未通知协作方前擅自切换
